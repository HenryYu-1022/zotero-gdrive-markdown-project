{
  "_comment_01": "这是标准 JSON 配置文件，不能写 // 或 /* */ 注释。This is standard JSON, so do not use // or /* */ comments.",
  "_comment_02": "请修改下面这些根级字段。Edit the root-level fields below this comment block.",
  "_comment_03": "_comment_fields 只是字段说明，不会被程序读取。_comment_fields is documentation only and is not read as runtime config.",
  "_comment_04": "路径尽量写绝对路径，尤其是 python_path、marker_cli、input_root、output_root、hf_home。Use absolute paths whenever possible, especially for python_path, marker_cli, input_root, output_root, and hf_home.",
  "_comment_05": "macOS 只使用 python_path；pythonw_path 仅供 Windows 后台计划任务优先使用。macOS only uses python_path; pythonw_path is mainly for Windows background scheduled tasks.",
  "_comment_fields": {
    "input_root": "源 PDF 根目录；CLI 和 Zotero 插件会用它计算相对路径。Root directory containing source PDFs; the CLI and Zotero plugin use it for relative paths.",
    "output_root": "输出根目录；程序会在这里写入 markdown、marker_raw、logs、state。Root output directory for markdown, marker_raw, logs, and state.",
    "python_path": "Python 可执行文件绝对路径；macOS LaunchAgent 依赖它，Windows 也可把它当作 pythonw_path 的回退值。Absolute path to the Python executable; required by macOS LaunchAgent and can also act as a Windows fallback for pythonw_path.",
    "pythonw_path": "Windows 专用后台运行路径，通常填写 pythonw.exe；macOS 不读取这个字段。Preferred hidden-background Python path on Windows, usually pythonw.exe; macOS does not read this field.",
    "model_cache_dir": "可选；supervisor 会把它导出成 MODEL_CACHE_DIR。Optional; exported by the supervisor as MODEL_CACHE_DIR.",
    "marker_cli": "Marker 命令名或绝对路径，例如 marker_single 或 /absolute/path/to/marker_single。Marker command name or absolute path, such as marker_single or /absolute/path/to/marker_single.",
    "hf_home": "Hugging Face 缓存目录。Hugging Face cache directory.",
    "marker_repo_root": "可选；只有需要从特定 Marker 源码目录启动时才填写。Optional; only needed if Marker must run from a specific source checkout.",
    "torch_device": "运行设备：NVIDIA 常用 cuda，Apple Silicon 常用 mps，无加速时用 cpu。Execution device: cuda for NVIDIA, mps for Apple Silicon, or cpu when no accelerator is available.",
    "output_format": "Marker 输出格式，通常保持 markdown。Marker output format; usually keep this as markdown.",
    "force_ocr": "是否强制 OCR；扫描版 PDF 或识别效果不好时可设为 true。Whether to force OCR; set true for scanned PDFs or weak extraction results.",
    "disable_image_extraction": "是否禁用图片提取。Whether to disable image extraction.",
    "disable_multiprocessing": "是否禁用多进程；排查兼容性问题时可改为 true。Whether to disable multiprocessing; set true when debugging compatibility issues.",
    "paginate_output": "是否在 markdown 中加入分页标记。Whether to add page markers to markdown output.",
    "compute_sha256": "是否计算 PDF 的 SHA256 并写入 frontmatter，用于跨设备的变更检测；默认 true。Whether to compute the PDF's SHA256 and embed it in the frontmatter for cross-device change detection; defaults to true.",
    "log_level": "日志级别，例如 INFO、WARNING、ERROR。Log level, such as INFO, WARNING, or ERROR.",
    "daemon_idle_timeout_seconds": "插件启动的 daemon 空闲多少秒后自动退出；0 表示不自动退出。How long the plugin daemon stays alive while idle; 0 disables automatic exit.",
    "run_mode": "运行模式：all-in-one（单机全功能，默认）/ runner（Win 端，只转换，不建镜像）/ controller（Mac 端，只巡检清理，不需要 marker）。Run mode: all-in-one (default) / runner (conversion only, no collection mirror) / controller (verification and cleanup only, marker not required).",
    "orphan_grace_hours": "controller 模式：PDF 消失后等待多少小时再归档对应 Markdown，默认 24。Controller mode: how many hours to wait before archiving a Markdown whose PDF has disappeared, default 24.",
    "archive_before_delete": "controller 模式：删除孤儿 Markdown 前是否先归档到 archive/，默认 true。Controller mode: whether to move orphan bundles to archive/ before deletion, default true.",
    "watch_initial_scan": "runner 端实时监控启动时是否先扫描并转换已有 PDF。Whether the runner watcher should scan existing PDFs at startup.",
    "watch_stable_checks": "runner 端转换前连续看到文件大小和 mtime 不变的次数。Number of stable file checks before converting a watched PDF.",
    "watch_stable_interval_seconds": "runner 端文件稳定性检查间隔秒数。Seconds between stable file checks.",
    "watch_poll_interval_seconds": "runner 端处理事件队列的轮询间隔秒数。Seconds between processing pending watcher events.",
    "zotero_db_path": "可选；Zotero 数据库 zotero.sqlite 的路径，配置后启用 collection 层级同步。Optional; path to Zotero's zotero.sqlite database, which enables collection hierarchy syncing.",
    "collection_mirror_mode": "可选；镜像模式为 symlink 或 copy。Optional; mirror mode can be symlink or copy.",
    "zotero_sync_interval_seconds": "可选；sync daemon 轮询 Zotero 数据库的间隔秒数，默认 60。Optional; polling interval in seconds for the Zotero sync daemon, default 60."
  },
  "run_mode": "all-in-one",
  "orphan_grace_hours": 24,
  "archive_before_delete": true,
  "input_root": "/Users/yourname/Documents/paper-library/input",
  "output_root": "/Users/yourname/Documents/paper-library/output",
  "python_path": "/opt/homebrew/bin/python3",
  "pythonw_path": "",
  "model_cache_dir": "/Users/yourname/.cache/marker/datalab_model_cache",
  "marker_cli": "/absolute/path/to/marker_single",
  "hf_home": "/Users/yourname/.cache/huggingface",
  "marker_repo_root": "",
  "torch_device": "mps",
  "output_format": "markdown",
  "force_ocr": true,
  "disable_image_extraction": false,
  "disable_multiprocessing": false,
  "paginate_output": false,
  "compute_sha256": true,
  "log_level": "INFO",
  "daemon_idle_timeout_seconds": 300,
  "watch_initial_scan": true,
  "watch_stable_checks": 3,
  "watch_stable_interval_seconds": 2,
  "watch_poll_interval_seconds": 1,
  "zotero_db_path": "",
  "collection_mirror_mode": "symlink",
  "zotero_sync_interval_seconds": 60
}
